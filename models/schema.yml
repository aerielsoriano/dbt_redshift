version: 2

models:
  - name: foo
    description: One record per customer
    columns:
      - name: id
        description: Primary key
        tests:
          - unique
          - not_null
      - name: name
        description: random name

  - name: model_version
    latest_version: 2
    columns:
      - name: id
      - name: name
      - name: color